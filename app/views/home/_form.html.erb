<%= simple_form_for(:home, :url => home_path, :method => :post, html: { class: 'form-inline' }) do |f| %>
	<%= f.input :user_text, label: 'Перевод: ', required: false %>
	<%= f.input :id_code, :as => :hidden, :input_html => { :value => @card.id }%>
	<%= f.button :submit, 'Проверить' %>
<% end %>
