<%= simple_form_for(:session, url: {action: 'create'}, :method => :post) do |f| %>
	<%= f.input :email, label: 'Email:', required: false %>
	<%= f.input :password, label: 'Пароль:', required: false %>
	<%= f.button :submit, 'Войти' %>
<% end %>
